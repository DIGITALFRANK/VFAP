version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - echo "In Install"
      - apt-get install jq -y
  pre_build:
    commands:
      - echo "In Pre_build"
      - sh $CODEBUILD_SRC_DIR/increment_version.sh -m 1.1.0
  build:
    commands:
      - echo "VFAP repo Build"
  post_build:
    commands:
      - echo "Post Build Actions"
artifacts:
  files:
    - main.json
    